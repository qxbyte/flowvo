package org.xue.agents.config;

import org.springframework.context.annotation.Configuration;

/**
 * 向量存储配置类
 * 使用Spring AI的自动配置功能
 */
@Configuration
public class VectorStoreConfig {
    
    // Spring AI 会根据application.yml中的配置自动创建 VectorStore Bean
    // 无需手动配置
} 